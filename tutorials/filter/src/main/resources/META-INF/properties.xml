<?xml version="1.0"?>
<configuration>
  <!-- 
  <property>
    <name>dt.application.{appName}.operator.{opName}.prop.{propName}</name>
    <value>some-default-value (if value is not specified, it is required from the user or custom config when launching)</value>
  </property>
  -->
  <!-- memory assigned to app master
  <property>
    <name>dt.attr.MASTER_MEMORY_MB</name>
    <value>1024</value>
  </property>
  -->

  <!-- these values override those set in populateDAG -->
  <property>
    <name>dt.application.FilterExample.operator.dataGenerator.prop.sleepTime</name>
    <value>100</value>
  </property>
  <property>
    <name>dt.application.FilterExample.operator.filterOperator.prop.condition</name>
    <value>({$}.getAmount() >= 20000)</value>
  </property>
  <property>
    <name>dt.application.FilterExample.operator.filteredInConsole.prop.stringFormat</name>
    <value>IN:%s</value>
  </property>
  <property>
    <name>dt.application.FilterExample.operator.filteredOutConsole.prop.stringFormat</name>
    <value>OUT:%s</value>
  </property>
</configuration>

